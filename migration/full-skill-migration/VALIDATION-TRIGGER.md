# Full skill migration validation trigger

This marker exists only to trigger validation of the persisted materialization after the GitHub Actions bot commit, which does not recursively trigger workflows through `GITHUB_TOKEN`.

Last persisted materialization requested for validation: `ba587827fcdc21e6326c1bee2ceb637d801e353f`.
